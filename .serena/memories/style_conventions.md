Conventions from AGENTS.md: Vue 3 Composition API with <script setup>; component files use PascalCase; props are camelCase; v-model uses modelValue and update:modelValue; Tailwind utilities preferred, custom classes prefixed ui-; path alias @/ -> src/; ESLint uses Airbnb base + Vue 3 recommended + Prettier. Reuse existing utils and patterns, keep diffs small, no new dependencies without explicit request.